name: Test

on: [push]

jobs:
  test:
    strategy:
      matrix:
        target: ['22']
    uses: snapshot-labs/actions/.github/workflows/test.yml@main
    secrets: inherit
    with:
      target: ${{ matrix.target }}
      postgres_database_name: snapshot_webhook_test
      postgres_schema_path: ''
